MemoryFS installed (namespace {{ .Release.Namespace }}).

Label nodes (if not done): kubectl label node <name> memoryfs.io/node=true

Pods:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

Dashboard:
  http://{{ include "memoryfs.fullname" . }}:{{ .Values.service.httpPort }}{{ .Values.dashboard.uriPrefix }}/dashboard

Port-forward:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "memoryfs.fullname" . }} 8080:{{ .Values.service.httpPort }}
{{- if .Values.mount.enabled }}

FUSE mount: {{ .Values.mount.hostPath }}
{{- end }}
