{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "memoryfs.serviceAccountName" . }}
  labels:
    {{- include "memoryfs.labels" . | nindent 4 }}
{{- end }}
