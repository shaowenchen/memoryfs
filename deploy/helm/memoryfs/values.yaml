# Default values for MemoryFS Helm chart.

replicaCount: 3

image:
  repository: shaowenchen/memoryfs
  tag: latest
  pullPolicy: IfNotPresent

nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  name: ""

# Chunk replication across nodes.
replicaFactor: 2

node:
  # Empty = auto from diskSync: memory (off) / buffered (on)
  chunkBackend: ""
  memCacheMB: 512
  diskQuotaGB: 0
  gcInterval: 5m
  # Periodic chunk flush/fsync to node.dataDir (emptyDir or PVC).
  diskSync:
    enabled: false
    interval: 30s
  defaultTTL: "0"
  maxFileAge: "0"
  dataDir: /data
  chunkDir: /data/chunks

  resources:
    requests:
      cpu: 500m
      memory: 1Gi
    limits:
      cpu: "2"
      memory: 4Gi

  # false = emptyDir; true = PVC volumeClaimTemplate
  persistence:
    enabled: false
    storageClass: ""
    size: 100Gi
    accessModes:
      - ReadWriteOnce

  podManagementPolicy: OrderedReady
  updateStrategy: RollingUpdate

  # Lifecycle hooks for safe rolling updates.
  lifecycle:
    preStopDrain: true
    postStartReady: true

  podDisruptionBudget:
    enabled: true
    minAvailable: 2

mount:
  enabled: false
  mountPath: /mnt/memoryfs
  hostPath: /var/lib/memoryfs
  replicaFactor: 2

metrics:
  enabled: false
  interval: 30s

dashboard:
  uriPrefix: /memoryfs

apiToken: ""

service:
  type: ClusterIP
  httpPort: 8080
  grpcPort: 9090
  raftPort: 8081

nodeSelector: {}
tolerations: []
affinity: {}
